org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# JDK: set org.gradle.java.home in ~/.gradle/gradle.properties if Gradle must use a
# specific JDK 21 install; do not commit machine paths here (breaks other machines).
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
